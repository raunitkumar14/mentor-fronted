// Bucket length already carries magnitude; opacity carries a second signal —
// the rarer, more-called buckets (the anomaly worth a manager's attention)
// read bolder even though their bars are short. Shared by the "Calls per
// Lead" panel and its duplicate inside Call Analytics.
export const BUCKET_OPACITY = { "0": 0.35, "1": 0.55, "2": 0.75, "3+": 1 };
